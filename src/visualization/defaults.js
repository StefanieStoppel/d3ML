const defaultOptions = {
  rootNode: 'body',
  width: 500,
  height: 300,
  padding: 50,
  backgroundColor: '#1d1e22',
  circleRadius: 5,
  circleFill: 'yellow',
  circleStroke: 'white'
};
const defaultType = 'None';

const defaultTypes = {
  A: 'A',
  B: 'B',
  None: 'None'
};

const defaultK = 3;

export default {
  defaultOptions,
  defaultType,
  defaultTypes,
  defaultK
};
